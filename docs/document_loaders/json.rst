============================================
JSON
============================================

In order to use ``JSONReader`` you need to install the ``jq`` package.

.. code-block:: bash

    pip install jq

.. automodule:: tempestai.document_loaders.json
    :members:
